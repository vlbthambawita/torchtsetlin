# C-5: how much does PLAN 7.1's validation-selected protocol cost us against the statistic the
# TM image literature actually reports (mean of the last 25 epochs, or a peak)?
# Implemented by keeping the last 25 automata snapshots and scoring them AFTER the loop, so no
# test accuracy exists while any decision is being made.
ctm-small|--test-curve-last 25
