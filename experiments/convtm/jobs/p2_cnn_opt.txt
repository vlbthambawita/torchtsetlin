# P2 wave 4 -- OPTIONAL, 1 seed. Run only if the card is still free after waves 1-3.
#
#   python code/cnn/queue_cnn.py --gpu 1 --jobs jobs/p2_cnn_opt.txt --seeds 0
#
# At 3 seeds these three priced at 23 GPU-h -- 36% of the whole of P2 -- for variants that
# REFINE the binary decomposition rather than establish it. `cnn-binary` and
# `cnn-binary-boolean` (wave 1) establish it.
#
#   cnn-binaryconnect  binary WEIGHTS, real activations -> splits the two binarisations
#   cnn-binary-xnor    + L1 scaling factors             -> the strongest binary arm
#   cnn-binary-aug     BNN with crop+flip               -> augmentation on a binary substrate
#
# A 1-seed result is reported WITH its seed count and may not enter a headline comparison
# (CHARTER: 3 seeds for anything in a comparison). It is a direction, not a difference.
cnn-binaryconnect|
cnn-binary-xnor|
cnn-binary-aug|
