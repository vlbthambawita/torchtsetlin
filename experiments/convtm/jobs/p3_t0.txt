# P3 rung T0 — the mandatory reproduction, cheapest reading first.
#
# ctm-vanilla-8k is 8000 clauses in TOTAL (the unqualified reading several later papers use);
# ctm-vanilla is 8000 PER CLASS = 80000 total, the 2019 paper's own reading [FACT: LG-010 A1].
# [MEASURED: calibration_throughput.json] 78 s/epoch vs 632 s/epoch -> 1.3 h vs 10.5 h per seed
# at 60 epochs. Run the cheap reading first; it establishes whether the harness reaches the
# 55-61% T0 band at all, for 4 GPU-h instead of 30.
#
# Both arms are config_status=provisional until P1 supplies the paper's T and s; their records
# are valid but record.admissible_as_reproduction() refuses them as reproduction evidence.
ctm-vanilla-8k|
