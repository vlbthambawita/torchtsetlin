# HOG specialist at its ONLY reachable point (20 000 total). 40 000 and 80 000 OOM a 24 GB card
# (AUDIT A23). Reported as a LEVEL comparison against ctm-therm5 at the same clause count,
# where the published values are HOG 64.2 vs Colour Thermometers 64.5 -- a near-equality that
# is itself a prediction worth testing. 2.04 GPU-h.
ctm-hog|--epochs 50 --eval-n 2000 --test-curve-last 25
