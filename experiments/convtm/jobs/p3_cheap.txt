# P3 arms that need zero or near-zero new code, all at 8000 total clauses / 10x10 / 60 epochs
# so that the clause and automata budgets are matched across the family.
# [MEASURED: calibration_throughput.json] ~78 s/epoch each -> ~1.3 GPU-h per seed.
# Uncomment as DR-001 admits them; each needs its paper's T and s before it is a reproduction.
ctm-vanilla-8k|
# ctm-coalesced|--n-clauses 8000 --patch 10
# ctm-weighted|--n-clauses 8000 --patch 10 --weighted true
# ctm-dropclause|--n-clauses 8000 --patch 10 --drop-clause-p 0.25
